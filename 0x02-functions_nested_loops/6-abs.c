#include "main.h"
/**
 * _abs - check absolute value
 * @n: integer to check
 * Return: n if positive and -n otherwise
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
