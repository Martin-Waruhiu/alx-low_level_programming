#include "main.h"

/**
 * mul - to multiply numbers
 * @a: - inter to multiply
 * @b: - interger to multiply with
 * Return: 0
 */

int mul(int a, int b)
{
	result = a * b;

	_putchar(result);

	return (0);
}
