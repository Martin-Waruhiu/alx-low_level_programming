#include "main.h"

/**
 * _abs - function to give absolute value of an interger
 * @n: the integer parameter
 * Return: - the value of of n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
